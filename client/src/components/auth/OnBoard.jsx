import { View, Text, StyleSheet } from "react-native";

const OnBoard = () => {
  return (
    <View style={styles.container}>
      <Text style={styles.onBoardText}>On board screen</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {},
  onBoardText: {},
});

export default OnBoard;
